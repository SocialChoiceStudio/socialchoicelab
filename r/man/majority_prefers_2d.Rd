% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{majority_prefers_2d}
\alias{majority_prefers_2d}
\title{Test whether a k-majority of voters prefer point A to point B}
\usage{
majority_prefers_2d(
  a,
  b,
  voter_ideals,
  dist_config = make_dist_config(),
  k = "simple"
)
}
\arguments{
\item{a}{Numeric vector of length 2: \code{c(x, y)} of point A.}

\item{b}{Numeric vector of length 2: \code{c(x, y)} of point B.}

\item{voter_ideals}{Flat numeric vector \code{[x0, y0, ...]} of voter
ideal points (length = 2 * n_voters).}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}

\item{k}{Majority threshold: \code{"simple"} for ⌊n/2⌋ + 1, or a positive
integer.}
}
\value{
Logical scalar: \code{TRUE} if at least \code{k} voters prefer A.
}
\description{
Voter \code{i} strictly prefers \code{A} iff
\code{d(ideal_i, A) < d(ideal_i, B)}. Ties in distance contribute 0 votes.
}
\examples{
\dontrun{
voters <- c(-1, -1, 1, -1, 1, 1)  # 3 voters
# A = (0,0) is closer to all 3 voters than B = (5,0).
majority_prefers_2d(c(0, 0), c(5, 0), voters)  # TRUE
}
}
