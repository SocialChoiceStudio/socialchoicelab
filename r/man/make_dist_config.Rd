% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_dist_config}
\alias{make_dist_config}
\title{Create a distance configuration}
\usage{
make_dist_config(
  distance_type = "euclidean",
  weights = NULL,
  n_dims = 2L,
  order_p = 2
)
}
\arguments{
\item{distance_type}{Character. One of \code{"euclidean"} (L2, default),
\code{"manhattan"} (L1), \code{"chebyshev"} (L∞), or
\code{"minkowski"} (general Lp).}

\item{weights}{Numeric vector of salience weights, one per dimension.
Default: all-ones vector of length \code{n_dims}.}

\item{n_dims}{Integer. Number of dimensions; used only when \code{weights}
is \code{NULL}.}

\item{order_p}{Numeric. Minkowski exponent p (>= 1); ignored for other
distance types.}
}
\value{
A named list accepted by all functions that take a distance
configuration argument.
}
\description{
Returns a named list understood by all C API wrappers that accept a distance
configuration. The list element order is fixed by contract with the C helper
\code{build_dist_config()} in \file{src/scs_r_helpers.h}; do not reorder.
}
\examples{
# Euclidean (default):
cfg <- make_dist_config()

# Manhattan with equal weights:
cfg_l1 <- make_dist_config(distance_type = "manhattan")

# Euclidean with asymmetric salience weights:
cfg_w <- make_dist_config(weights = c(2.0, 1.0))
}
