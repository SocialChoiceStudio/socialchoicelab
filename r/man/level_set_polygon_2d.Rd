% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{level_set_polygon_2d}
\alias{level_set_polygon_2d}
\title{Sample a 2D level set as a polygon in one call}
\usage{
level_set_polygon_2d(
  ideal_x,
  ideal_y,
  utility_level,
  loss_config = make_loss_config(),
  dist_config = make_dist_config(),
  n_samples = 64L
)
}
\arguments{
\item{ideal_x, ideal_y}{Ideal point coordinates.}

\item{utility_level}{Target utility level (same as \code{\link{level_set_2d}}).}

\item{loss_config}{Loss configuration from \code{\link{make_loss_config}}.}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}

\item{n_samples}{Integer. Sample count for smooth shapes; ignored for polygon
level sets (Manhattan / Chebyshev). Default: 64.}
}
\value{
Numeric matrix (n_pts × 2) with columns \code{x} and \code{y}.
}
\description{
Compound convenience: same result as \code{\link{level_set_2d}} followed by
\code{\link{level_set_to_polygon}}, without passing the intermediate
named list across the C boundary.
}
\examples{
\dontrun{
lc <- make_loss_config(loss_type = "linear", sensitivity = 1)
dc <- make_dist_config(distance_type = "euclidean", weights = c(1, 1))
poly <- level_set_polygon_2d(1, -0.5, utility_level = -0.5, lc, dc, n_samples = 32L)
plot(poly, type = "l", asp = 1)
}
}
