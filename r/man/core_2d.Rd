% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{core_2d}
\alias{core_2d}
\title{Compute the core in continuous 2D space}
\usage{
core_2d(voter_ideals, dist_config = make_dist_config(), k = "simple")
}
\arguments{
\item{voter_ideals}{Flat numeric vector of voter ideal coordinates.}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}

\item{k}{Majority threshold.}
}
\value{
Named list \code{list(found, x, y)}.
\code{found} is \code{TRUE} if the core is non-empty; \code{x} and
\code{y} are \code{NA} when \code{found == FALSE}.
}
\description{
The core is non-empty only when a Condorcet point exists in the policy
space. For most voter configurations the core is empty.
}
