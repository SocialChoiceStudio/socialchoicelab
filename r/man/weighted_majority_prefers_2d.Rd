% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{weighted_majority_prefers_2d}
\alias{weighted_majority_prefers_2d}
\title{Test whether the weighted majority prefers point A to point B}
\usage{
weighted_majority_prefers_2d(
  a,
  b,
  voter_ideals,
  weights,
  dist_config = make_dist_config(),
  threshold = 0.5
)
}
\arguments{
\item{a, b}{Numeric vectors of length 2: ideal-point coordinates.}

\item{voter_ideals}{Flat numeric vector of voter ideal coordinates.}

\item{weights}{Positive numeric vector of voter weights (length = n_voters).}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}

\item{threshold}{Weight fraction in \code{(0, 1]}. Use 0.5 for simple
weighted majority.}
}
\value{
Logical scalar.
}
\description{
Returns \code{TRUE} if the total weight of voters preferring A meets or
exceeds \code{threshold * sum(weights)}.
}
\examples{
\dontrun{
voters  <- c(-1, -1, 1, -1, 1, 1)
weights <- c(10, 1, 1)  # first voter has most weight
# A = (-0.5,-0.5) is close to voter 1; B = (2, 0) is not.
weighted_majority_prefers_2d(c(-0.5, -0.5), c(2, 0), voters, weights)
}
}
