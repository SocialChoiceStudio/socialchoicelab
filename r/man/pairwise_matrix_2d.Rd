% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{pairwise_matrix_2d}
\alias{pairwise_matrix_2d}
\title{Compute the n_alts × n_alts pairwise preference matrix}
\usage{
pairwise_matrix_2d(
  alts,
  voter_ideals,
  dist_config = make_dist_config(),
  k = "simple"
)
}
\arguments{
\item{alts}{Flat numeric vector \code{[x0, y0, ...]} of alternative
coordinates (length = 2 * n_alts).}

\item{voter_ideals}{Flat numeric vector of voter ideal coordinates.}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}

\item{k}{Majority threshold.}
}
\value{
Integer matrix (n_alts × n_alts) with row and column names
\code{"alt1", "alt2", ...}.
}
\description{
Entry \code{[i, j]} is \code{1} (WIN), \code{0} (TIE), or \code{-1}
(LOSS) from alternative \code{i}'s perspective. The matrix is
anti-symmetric: \code{M[i,j] = -M[j,i]}.
}
