% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{layer_preferred_regions}
\alias{layer_preferred_regions}
\title{Add voter preferred-to regions}
\usage{
layer_preferred_regions(
  fig,
  voters,
  sq,
  color_by_voter = FALSE,
  fill_color = NULL,
  line_color = NULL,
  palette = "auto",
  voter_names = NULL,
  name = "Preferred Region",
  theme = "dark2"
)
}
\arguments{
\item{fig}{A plotly figure from \code{\link{plot_spatial_voting}}.}

\item{voters}{Flat numeric vector of voter ideal points.}

\item{sq}{Numeric vector \code{c(x, y)} for the status quo.}

\item{color_by_voter}{Logical. \code{FALSE} (default): all circles share
one neutral colour. \code{TRUE}: each voter gets a unique colour from
\code{palette} shown individually in the legend.}

\item{fill_color}{Default fill colour. \code{NULL} uses the theme default.}

\item{line_color}{Default outline colour. \code{NULL} uses the theme default.}

\item{palette}{Palette name for \code{color_by_voter} mode.}

\item{voter_names}{Character vector of voter labels.}

\item{name}{Legend group label (when \code{color_by_voter = FALSE}).}

\item{theme}{Colour theme — see \code{\link{plot_spatial_voting}}.}
}
\value{
The updated plotly figure.
}
\description{
Draws a filled circle for each voter centred at their ideal point with
radius equal to the Euclidean distance to the status quo. The interior
is the set of policies that voter strictly prefers to the SQ.
}
\examples{
\dontrun{
voters <- c(-1.0, -0.5, 0.0, 0.0, 0.8, 0.6)
sq     <- c(0.1, 0.1)
fig <- plot_spatial_voting(voters, sq = sq)
fig <- layer_preferred_regions(fig, voters, sq)
fig
}
}
