% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{profile_build_spatial}
\alias{profile_build_spatial}
\title{Build an ordinal preference profile from a 2D spatial model}
\usage{
profile_build_spatial(alt_xy, voter_ideals, dist_config = make_dist_config())
}
\arguments{
\item{alt_xy}{Numeric vector \code{[x0, y0, x1, y1, ...]} of alternative
coordinates (length = 2 * n_alts).}

\item{voter_ideals}{Numeric vector \code{[x0, y0, ...]} of voter ideal
point coordinates (length = 2 * n_voters).}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}
}
\value{
A \code{\link{Profile}} object.
}
\description{
Each voter ranks alternatives by distance: closest first, ties broken by
smallest alternative index.
}
\examples{
\dontrun{
alts   <- c(0, 0, 2, 0, -2, 0)
voters <- c(-1, -1, 1, -1, 1, 1)
prof   <- profile_build_spatial(alts, voters)
prof$dims()
}
}
