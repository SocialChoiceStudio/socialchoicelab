% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voting_rules.R
\name{scoring_rule_one_winner}
\alias{scoring_rule_one_winner}
\title{Return a single winner under a positional scoring rule with tie-breaking}
\usage{
scoring_rule_one_winner(
  profile,
  score_weights,
  tie_break = "smallest_index",
  stream_manager = NULL,
  stream_name = NULL
)
}
\arguments{
\item{profile}{A \code{\link{Profile}} object.}

\item{score_weights}{Non-increasing numeric vector of length n_alts.
\code{score_weights[r]} is awarded to the alternative at rank \code{r}
(rank 1 = most preferred, 1-indexed).}

\item{tie_break}{\code{"smallest_index"} (default) or \code{"random"}.}

\item{stream_manager}{A \code{\link{StreamManager}} object; required when
\code{tie_break = "random"}.}

\item{stream_name}{Character. Named stream for randomness; required when
\code{tie_break = "random"}.}
}
\value{
Integer scalar (1-based).
}
\description{
Return a single winner under a positional scoring rule with tie-breaking
}
\examples{
\dontrun{
prof <- profile_build_spatial(c(0, 0, 2, 0, -2, 0), c(-1, -1, 1, -1, 1, 1))
scoring_rule_one_winner(prof, c(2, 1, 0))
}
}
