% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{load_scenario}
\alias{load_scenario}
\title{Load a built-in named scenario}
\usage{
load_scenario(name)
}
\arguments{
\item{name}{Character. Scenario identifier — one of the names returned by
\code{list_scenarios()} (e.g. \code{"laing_olmsted_bear"}).}
}
\value{
A named list with elements:
\describe{
\item{\code{name}}{Full display name (character).}
\item{\code{description}}{Short description (character).}
\item{\code{source}}{Bibliographic source (character).}
\item{\code{n_voters}}{Number of voters (integer).}
\item{\code{n_dimensions}}{Number of policy dimensions (integer).}
\item{\code{space}}{Named list with \code{x_range} and \code{y_range} (each length-2 numeric).}
\item{\code{decision_rule}}{Majority threshold, e.g. \code{0.5} (numeric).}
\item{\code{dim_names}}{Axis labels (character vector, length \code{n_dimensions}).}
\item{\code{voters}}{Flat numeric vector \code{[x0, y0, x1, y1, ...]} of length
\code{n_voters * n_dimensions}.}
\item{\code{voter_names}}{Character vector of length \code{n_voters}.}
\item{\code{status_quo}}{Numeric vector of length \code{n_dimensions}, or \code{NULL}.}
}
}
\description{
Reads a bundled JSON scenario file and returns a list ready to pass to
\code{plot_spatial_voting()} and the computation functions.
}
\examples{
sc <- load_scenario("laing_olmsted_bear")
sc$n_voters          # 5
sc$voters            # flat [x0, y0, x1, y1, ...]
sc$status_quo        # c(100, 100)

sc2 <- load_scenario("tovey_regular_polygon")
sc2$voter_names      # "XX0" .. "XX10"

}
