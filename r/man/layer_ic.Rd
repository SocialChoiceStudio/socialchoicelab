% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{layer_ic}
\alias{layer_ic}
\title{Add voter indifference curves}
\usage{
layer_ic(
  fig,
  voters,
  sq,
  color_by_voter = FALSE,
  fill_color = NULL,
  line_color = NULL,
  line_width = 1,
  palette = "auto",
  voter_names = NULL,
  name = "Indifference Curves",
  theme = "dark2"
)
}
\arguments{
\item{fig}{A plotly figure from \code{\link{plot_spatial_voting}}.}

\item{voters}{Flat numeric vector of voter ideal points.}

\item{sq}{Numeric vector \code{c(x, y)} for the status quo.}

\item{color_by_voter}{Logical. \code{FALSE} (default): all curves share a
single neutral colour with one legend entry. \code{TRUE}: each voter gets
a unique colour from \code{palette} shown individually in the legend.}

\item{fill_color}{Fill colour (CSS rgba string), or \code{NULL} for no fill
(default). Overrides theme when not \code{NULL}.}

\item{line_color}{Uniform line colour used when \code{color_by_voter = FALSE}.
\code{NULL} uses the theme default.}

\item{line_width}{Stroke width of the IC circles in pixels. Default \code{1}.}

\item{palette}{Palette name for \code{color_by_voter} mode. \code{"auto"}
(default) uses the \code{theme}'s palette.}

\item{voter_names}{Character vector of voter labels.}

\item{name}{Legend group label (when \code{color_by_voter = FALSE}).}

\item{theme}{Colour theme — see \code{\link{plot_spatial_voting}}.}
}
\value{
The updated plotly figure.
}
\description{
Draws a circle for each voter centred at their ideal point with radius
equal to the Euclidean distance to the status quo. The circle is the
voter's indifference curve through the SQ.
}
\examples{
\dontrun{
voters <- c(-1.0, -0.5, 0.0, 0.0, 0.8, 0.6)
sq     <- c(0.1, 0.1)
fig <- plot_spatial_voting(voters, sq = sq)
fig <- layer_ic(fig, voters, sq)
fig
}
}
