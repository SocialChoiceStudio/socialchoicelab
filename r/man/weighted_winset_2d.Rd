% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/winset.R
\name{weighted_winset_2d}
\alias{weighted_winset_2d}
\title{Compute the weighted-majority 2D winset of a status quo}
\usage{
weighted_winset_2d(
  status_quo,
  voter_ideals,
  weights,
  threshold = 0.5,
  dist_config = make_dist_config(),
  n_samples = 64L
)
}
\arguments{
\item{status_quo}{Numeric vector of length 2.}

\item{voter_ideals}{Numeric vector \code{[x0, y0, ...]} of length 2 * n_voters.}

\item{weights}{Positive numeric vector of voter weights (length = n_voters).}

\item{threshold}{Weight fraction in (0, 1] required for majority. Use 0.5
for simple weighted majority.}

\item{dist_config}{Distance configuration from \code{\link{make_dist_config}}.}

\item{n_samples}{Boundary quality (>= 4). Default: 64.}
}
\value{
A \code{\link{Winset}} object.
}
\description{
Like \code{\link{winset_2d}} but with voter weights: a policy beats the
status quo if the total weight of its supporters meets or exceeds
\code{threshold * sum(weights)}.
}
\examples{
\dontrun{
voters  <- c(-1, -1, 1, -1, 1, 1)
weights <- c(10, 1, 1)
ws <- weighted_winset_2d(c(0, 0), voters, weights)
ws$is_empty()
}
}
