% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voter_sampler.R
\name{draw_voters}
\alias{draw_voters}
\title{Draw voter ideal points from a spatial distribution}
\usage{
draw_voters(n, sampler, stream_manager, stream_name, n_dims = 2L)
}
\arguments{
\item{n}{Integer scalar. Number of voters to draw (>= 1).}

\item{sampler}{A \code{VoterSamplerConfig} object from
\code{\link{make_voter_sampler}}.}

\item{stream_manager}{A \code{\link{StreamManager}} object.}

\item{stream_name}{Character scalar. Name of the stream to use for
randomness (will be registered if not already present).}

\item{n_dims}{Integer scalar. Number of spatial dimensions (currently only
2 is supported).}
}
\value{
Numeric vector of length \code{n * n_dims} in row-major order
\code{[x0, y0, x1, y1, ...]}.
}
\description{
Generates a flat numeric vector of voter ideal-point coordinates using the
distribution specified in \code{sampler}.  The result is in row-major
interleaved order: \code{[x0, y0, x1, y1, ...]}, which is the format
expected by \code{\link{competition_run}} and
\code{\link{profile_build_spatial}}.
}
\examples{
\dontrun{
sm     <- stream_manager(42L, "voters")
s      <- make_voter_sampler("gaussian", mean = 0, sd = 0.4)
voters <- draw_voters(100L, s, sm, "voters")
# voters is a length-200 numeric vector ready for competition_run()
}
}
