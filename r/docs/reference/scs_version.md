# Query the compiled C API version

Query the compiled C API version

## Usage

``` r
scs_version()
```

## Value

Named list `list(major, minor, patch)`.

## Examples

``` r
if (FALSE) { # \dontrun{
# Requires libscs_api to be built. See the package README.
scs_version()
} # }
```
